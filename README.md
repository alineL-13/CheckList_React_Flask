# CheckList_React_Flask
A full-stack CheckList application for managing simple tasks. Features include creating tasks, validating required fields, listing tasks sorted by due date, updating task status, deleting tasks, and clearing all completed tasks. Data is persisted using SQLite with a Flask + SQLAlchemy backend. Frontend built with React and TailwindCSS.
